# ============================================================
# SmartCityNet — dependencias Python (backend + API FastAPI)
# ============================================================
# El frontend es React (carpeta web/, se instala aparte con npm — ver
# web/README.md). Este archivo cubre el backend VANET y la API que lo expone.

# --- Núcleo backend VANET ---
requests>=2.28.0          # descarga de mapas desde OpenStreetMap
numpy>=1.23.0             # matrices de conectividad (multisalto)
pyproj>=3.4               # proyección exacta SUMO(UTM) → lat/lon

# --- API REST (la consume el frontend React) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- Optimización de despliegue de RSU (docplex/CPLEX) ---
# docplex construye/resuelve el modelo; el motor 'cplex' viene con IBM CPLEX
# Optimization Studio. La edición community (pip install cplex) se limita a 1000
# variables/restricciones; para el problema real usa el motor COMPLETO (licencia
# académica gratuita de IBM), que solo soporta Python 3.7–3.10 → de ahí 3.10.
docplex>=2.25
cplex>=22.1 ; python_version < "3.13"
